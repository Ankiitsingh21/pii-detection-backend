class Imageservice{
        async Image(image){
                try {
                        
                } catch (error) {
                        console.log("Error in service layer",error);
                        return error;
                }
        }
}

module.exports=Imageservice;